/* Condoções - if E ELSE */
var nota 1 = 6.5;
var nota 2 = 7 ;
var nota 3 = 10
var nota4 = 9.3;
var media = (nota + nota2 + nota3 + nota4) / 4;
console.log(' A media do aluno é ${media} ' ) ;
var situacao =''
if(media >= 7){
    situacao = 'Aprovado";'
else ir{}
}else if (media < 5){
    situacao = 'Reprovado' ;
} else {
    situacao = 'Recuperação';
}
console.log(' O aluno está ${situcao}`)

